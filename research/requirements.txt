transformers==4.51.3
numpy==1.24.4
scipy==1.14.0
matplotlib==3.10.3
safetensors==0.4.5
huggingface-hub==0.36.2
# Optional browser validation: playwright; then python -m playwright install chromium
# Install a CUDA-enabled torch separately; see environment.json for the measured build.
