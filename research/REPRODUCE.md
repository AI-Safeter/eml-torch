# Reproduce the archived experiments

The superseded experiment runners were removed from the current tree. Use the complete historical checkout and its original instructions:

```bash
git worktree add --detach ../emltorch-arithmetic-archive 51cb441c80ef6eef5b2fe4480b1c6b45ec896cf5
```

Read `research/REPRODUCE.md` in that worktree. Its manifest covers the original source and evidence. The result summaries, raw observations, checkpoints and explorer retained here have not been removed.
